// import { flush, render } from "@stencil/core/testing";
import { LayoutEditor } from "./layout-editor";

describe("gx-layout-editor", () => {
  it("should build", () => {
    // expect(new LayoutEditor()).toBeTruthy();
  });
});
